# 🎬 Оптимизация видео для Hero секции

## 📦 Что у вас есть:
- `/public/video-hero-trim.mp4` - исходное видео
- `/public/hero-poster.jpg` - постер (превью)

## ⚡ Оптимизация (опционально, если видео тормозит)

### Вариант 1: Конвертация в WebM (лучшее сжатие)

Установите FFmpeg (если ещё нет):
```bash
# macOS
brew install ffmpeg

# или скачайте с https://ffmpeg.org/download.html
```

Конвертируйте видео:
```bash
cd public

# WebM формат (лучшее сжатие, ~50% меньше веса)
ffmpeg -i video-hero-trim.mp4 \
  -c:v libvpx-vp9 \
  -crf 35 \
  -b:v 0 \
  -row-mt 1 \
  -an \
  video-hero-trim.webm

# Или H.265 (HEVC) для меньшего размера MP4
ffmpeg -i video-hero-trim.mp4 \
  -c:v libx265 \
  -crf 28 \
  -preset slow \
  -an \
  video-hero-optimized.mp4
```

### Вариант 2: Уменьшить разрешение (для мобильных)

```bash
# 1080p версия (для десктопа)
ffmpeg -i video-hero-trim.mp4 \
  -vf scale=1920:1080 \
  -c:v libx264 \
  -crf 23 \
  -preset medium \
  -an \
  video-hero-1080p.mp4

# 720p версия (для мобильных)
ffmpeg -i video-hero-trim.mp4 \
  -vf scale=1280:720 \
  -c:v libx264 \
  -crf 23 \
  -preset medium \
  -an \
  video-hero-720p.mp4
```

### Вариант 3: Оптимизация постера

```bash
# Конвертируем постер в WebP (меньше вес)
ffmpeg -i hero-poster.jpg \
  -c:v libwebp \
  -quality 80 \
  hero-poster.webp

# Или создать постер из первого кадра видео
ffmpeg -i video-hero-trim.mp4 \
  -ss 00:00:01 \
  -vframes 1 \
  -q:v 2 \
  hero-poster.jpg
```

## 🎯 После оптимизации обновите код:

Если создали WebM версию, добавьте в Hero:
```tsx
<video ...>
  <source src="/video-hero-trim.webm" type="video/webm" />
  <source src="/video-hero-trim.mp4" type="video/mp4" />
</video>
```

Если создали WebP постер:
```tsx
<video poster="/hero-poster.webp" ...>
```

## 📊 Рекомендуемые параметры:

- **Разрешение**: 1920x1080 (Full HD) или 1280x720 (HD)
- **Битрейт**: 2-5 Mbps для MP4, 1-3 Mbps для WebM
- **FPS**: 24-30 fps (больше не нужно для фона)
- **Аудио**: удалить (не нужно для фонового видео)
- **Длина**: 10-20 секунд loop (достаточно)

## 🚀 Результат:
- Исходный файл: ~10-50 MB
- После оптимизации: ~2-10 MB
- Скорость загрузки: в 3-5 раз быстрее!

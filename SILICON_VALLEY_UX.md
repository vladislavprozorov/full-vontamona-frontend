# 🎨 Silicon Valley UX Best Practices для Quiz

## ✅ Что уже реализовано (уровень FAANG)

### 1. **Progressive Disclosure** (Apple, Airbnb)

- ✅ Один вопрос за раз
- ✅ Прогресс-бар вместо номеров страниц
- ✅ Минимальный когнитивный груз

### 2. **Forgiving Inputs** (Stripe, Google)

- ✅ Автоформатирование телефона
- ✅ Нет блокировки пропустить приоритеты
- ✅ Опция "Пока не определился" вместо кнопки "Пропустить"

### 3. **Immediate Feedback** (Apple, Linear)

- ✅ Динамические микро-подсказки
- ✅ Emerald green для успеха (не агрессивный зелёный)
- ✅ Neutral grey для нейтральных состояний

### 4. **Conversational Tone** (Stripe, Notion)

- ✅ "Вы можете выбрать" вместо "Можно выбрать"
- ✅ Вопросы через "для вас" (персонализация)
- ✅ Нет технического жаргона

### 5. **Loading States** (Apple, Vercel)

- ✅ Анимированный спиннер при отправке
- ✅ Disabled state с opacity
- ✅ Текст "Отправка..." для контекста

### 6. **Micro-interactions** (Apple, Stripe)

- ✅ `active:scale-[0.98]` на всех кнопках
- ✅ Плавные transitions
- ✅ Hover states с subtle feedback

### 7. **Copy Success Pattern** (GitHub, Vercel)

- ✅ Иконка копирования
- ✅ Визуальный feedback (галочка 2 сек)
- ✅ Нет alert() — всё inline

---

## 🚀 Следующий уровень (что можно добавить)

### 1. **Keyboard Navigation** (Apple, Linear)

```tsx
// Для каждой кнопки варианта
onKeyDown={(e) => {
  if (e.key === 'Enter' || e.key === ' ') {
    e.preventDefault();
    onSelect(option);
  }
}}
tabIndex={0}
```

**Зачем:**

- Power users любят клавиатуру
- Accessibility (a11y)
- Профессиональное ощущение

### 2. **Focus Management** (Apple, Stripe)

```tsx
// После перехода на новый шаг
useEffect(() => {
  // Скроллим наверх формы
  scrollableRef.current?.scrollTo({ top: 0, behavior: "smooth" });

  // Фокус на первую кнопку
  setTimeout(() => firstButtonRef.current?.focus(), 100);
}, [currentStep]);
```

**Зачем:**

- Пользователь сразу готов к действию
- Не нужно искать мышью

### 3. **Smart Defaults** (Stripe, Superhuman)

```tsx
// Если пользователь выбрал "400 000 ₽+" в бюджете
// → автоматически подсвечиваем "Комфорт и сервис" в приоритетах
// (но не выбираем, просто subtle border)
```

**Зачем:**

- Помогаем принять решение
- Показываем понимание

### 4. **Undo Pattern** (Gmail, Linear)

```tsx
// После выбора варианта → показываем 2 сек
<div className="fixed bottom-4 right-4">
  Выбрано: "Средиземное море"
  <button onClick={undo}>Отменить</button>
</div>
```

**Зачем:**

- Уверенность пользователя
- Можно быстро исправить

### 5. **Error Recovery** (Stripe)

```tsx
// Если submit fail
<div className="text-red-600 text-sm">
  Не удалось отправить. <button onClick={retry}>Попробовать снова</button>
  или <a href="tel:+7...">позвоните нам</a>
</div>
```

**Зачем:**

- Никогда не оставляй пользователя в тупике
- Альтернативный путь всегда

### 6. **Skeleton Loading** (Vercel, Linear)

```tsx
// Вместо пустого экрана при загрузке
<div className="space-y-3">
  <div className="h-4 bg-neutral-200 rounded animate-pulse w-3/4" />
  <div className="h-12 bg-neutral-200 rounded animate-pulse" />
  <div className="h-12 bg-neutral-200 rounded animate-pulse" />
</div>
```

**Зачем:**

- Perceived performance
- Нет ощущения "зависло"

### 7. **Exit Intent** (Superhuman, Notion)

```tsx
// Если пользователь уходит на шаге 3+
// → показываем модалку:
"Ваш прогресс будет потерян. Уверены?"[Остаться][Выйти];
```

**Зачем:**

- Сохраняем конверсию
- Уважаем время пользователя

### 8. **Animation Consistency** (Apple)

```tsx
// Единый duration для всех transitions
const ANIMATION = {
  duration: 200,
  ease: 'ease-out',
}

// Используй везде
transition: all ${ANIMATION.duration}ms ${ANIMATION.ease}
```

**Зачем:**

- Единое ощущение системы
- Профессиональный polish

### 9. **Empty States** (Stripe, Notion)

```tsx
// Если приоритеты не выбраны
<div className="py-8 text-center">
  <p className="text-neutral-500 mb-2">Пока ничего не выбрано</p>
  <p className="text-xs text-neutral-400">
    Выберите что важно или переходите к контактам
  </p>
</div>
```

**Зачем:**

- Нет "пустого экрана"
- Направляем действие

### 10. **Haptic Feedback** (iOS Safari)

```tsx
// При выборе варианта (на мобильных)
if ("vibrate" in navigator) {
  navigator.vibrate(10); // 10ms vibration
}
```

**Зачем:**

- Tactile feedback
- Premium feel на mobile

---

## 🎯 Priority для внедрения (по ROI)

### High Impact, Low Effort:

1. ✅ Keyboard navigation (Enter/Space)
2. ✅ Focus management (auto-scroll + focus)
3. ✅ Error recovery с retry button

### High Impact, Medium Effort:

4. ✅ Skeleton loading
5. ✅ Undo toast notification
6. ✅ Animation consistency

### Medium Impact, Low Effort:

7. ✅ Empty states
8. ✅ Haptic feedback (mobile)

### Low Priority (nice-to-have):

9. Smart defaults (требует логики)
10. Exit intent (может раздражать)

---

## 📏 Метрики для отслеживания

### Conversion Rate:

- Сколько % заполняют до конца
- На каком шаге отваливаются

### Time to Complete:

- Идеал: < 60 секунд
- Если > 90 сек → что-то не так

### Error Rate:

- Сколько % получают ошибку валидации
- Если > 5% → упрости поля

### Return Rate:

- Сколько возвращаются назад по кнопке "← Назад"
- Если часто → вопросы непонятны

---

## 💎 Luxury UX Checklist (Apple/Airbnb level)

- ✅ Анимации < 300ms (быстрые, не медленные)
- ✅ Никаких резких движений
- ✅ Consistent spacing (всегда кратно 4px)
- ✅ Одна акцентная кнопка на экран
- ✅ Neutral palette (не кричащие цвета)
- ✅ Subtle shadows (не Drop Shadow из 2010)
- ✅ Rounded corners (но не overboard)
- ✅ Breathing room (white space)
- ✅ Predictable behavior (нет сюрпризов)
- ✅ Forgiving errors (можно исправить)

---

## 🔥 Золотые правила Silicon Valley UX

1. **"Don't make me think"** (Steve Krug)
   - Если нужно объяснять — переделай

2. **"Reduce, reuse, recycle"** (Dieter Rams)
   - Убирай всё лишнее

3. **"Fast by default"** (Vercel)
   - < 200ms ощущается instant

4. **"Progressive enhancement"** (Apple)
   - Сначала работает, потом красиво

5. **"Be consistent, not uniform"** (IBM Design)
   - Единые паттерны, разные контексты

6. **"Anticipate mistakes"** (Stripe)
   - Предотвращай ошибки до того как случились

7. **"Delight, don't annoy"** (Superhuman)
   - Микро-радости, не отвлечения

8. **"Exit > Entry"** (Apple)
   - Выход из флоу важнее входа

9. **"Mobile isn't small desktop"** (Luke W)
   - Разные контексты, разные решения

10. **"Measure twice, cut once"** (Basecamp)
    - Тестируй на реальных людях

---

**Итог:** Твой квиз уже на уровне хорошего стартапа. Добавь 3-5 пунктов из High Impact списка — будет уровень FAANG.

# Resend Setup Instructions

## 🚀 Настройка Resend для отправки email

### 1. Получить API ключ

1. Перейти на [resend.com](https://resend.com)
2. Зарегистрироваться / Войти
3. Перейти в **API Keys**
4. Создать новый ключ → Скопировать

### 2. Добавить ключ в .env.local

```bash
RESEND_API_KEY=re_ваш_реальный_ключ
EMAIL_FROM=onboarding@resend.dev  # для тестирования
# или
EMAIL_FROM=noreply@yourdomain.com # для продакшена
```

### 3. Верификация домена (для продакшена)

Если хотите отправлять с вашего домена (например, `noreply@vontamona.com`):

1. В Resend → **Domains** → Add Domain
2. Добавить ваш домен
3. Скопировать DNS записи (SPF, DKIM, DMARC)
4. Добавить их в настройках вашего домена
5. Подождать верификации

### 4. Тестирование

Для тестирования можно использовать встроенный тестовый домен Resend:

```env
EMAIL_FROM=onboarding@resend.dev
```

Письма будут приходить, но с предупреждением "sent via resend.dev".

### 5. Проверка работы

После добавления API ключа в `.env.local`:

1. Перезапустить dev server: `npm run dev`
2. Заполнить квиз
3. Проверить:
   - Telegram (должно прийти сообщение)
   - Email менеджера (info@vontamona.com)
   - Email клиента (автоответ)

### 6. Логирование

В консоли сервера будет:

- ✅ Manager email sent successfully
- ✅ Client auto-reply sent to: email@example.com

Или если ключ не настроен:

- ⚠️ Resend API key not configured
- 📧 Manager email preview: info@vontamona.com
- 📧 Client email would be sent to: email@example.com

---

## 📧 Текущее состояние

### ✅ Реализовано:

- Красивый HTML шаблон письма клиенту
- Письмо менеджеру с полной информацией о лиде
- Автоматический автоответ клиенту
- SLA подсказки в письме менеджеру
- Graceful fallback если ключа нет

### 📝 Что в письме клиенту:

- Персонализация (имя клиента)
- Номер заявки (крупно, в рамке)
- Описание процесса подбора
- Что будет дальше (пошагово)
- Контакты для обратной связи
- Luxury-tone (без кричащих цветов, минимализм)

### 📧 Что в письме менеджеру:

- Полная информация о клиенте
- Ответы квиза
- Скоринг с emoji
- SLA ("Связаться в течение 30 минут" и т.д.)
- Инсайты (что важно для клиента)

---

## 🎯 Следующие шаги

1. Получить API ключ от Resend
2. Добавить в .env.local
3. Протестировать отправку
4. (опционально) Верифицировать домен для продакшена

# ✅ Исправления выполнены

## 🔧 Исправлено:

### 1. TypeScript ошибки

✅ Добавлены явные типы для всех `.map()` функций:

- `app/privacy/page.tsx`
- `app/contract/page.tsx`
- `app/services/page.tsx`
- `app/legal/page.tsx`
- `components/legal/LegalNav.tsx`

### 2. Единая формулировка про оплату

**Везде используется:**

> **"Оплата производится после заключения индивидуального договора по Системе быстрых платежей (СБП)"**

**Где добавлена:**

#### `/contract` (Договор и оплата):

- ✅ В intro (начало страницы)
- ✅ В секции "Оплата услуг"

#### `/services` (Описание услуг):

- ✅ В разделе "Наши гарантии" (новый пункт)
- ✅ В разделе "Стоимость и оплата" (обновлён заголовок и добавлено предупреждение)
- ✅ В процессе работы (обновлены шаги 3-7)

#### Footer:

- ✅ Использует реквизиты из `config/company.ts`
- ✅ Отображает `COMPANY.legalName` и `COMPANY.inn`
- ✅ Ссылки на все документы

## 📄 Обновлённый процесс работы

### Старый (в /services):

1. Запрос
2. Консультация
3. Бронирование
4. **Оплата** (предоплата 30%)
5. Оформление
6. Доплата
7. Отправление

### Новый (в /services):

1. Запрос
2. Консультация
3. **Согласование** ← новое
4. **Договор** ← новое
5. **Оплата (после договора по СБП)** ← исправлено
6. Бронирование
7. Отправление

## 🎯 Единая логика везде:

### На странице `/contract`:

```
1. Консультация и подбор
2. Согласование условий
3. Подготовка договора
4. Подписание договора
5. Оплата (только после подписания)
```

### На странице `/services`:

```
1. Запрос
2. Консультация
3. Согласование
4. Договор
5. Оплата (после подписания по СБП)
6. Бронирование
7. Отправление
```

### В гарантиях:

- "Официальное оформление индивидуального договора"
- "Оплата производится после заключения договора"
- "Оплата по безналичному расчету (СБП) с выдачей документов"

## 🔗 Footer обновлён

```tsx
© {currentYear} {COMPANY.legalName}. Все права защищены.
ИНН {COMPANY.inn}

Ссылки:
- Политика конфиденциальности → /privacy
- Договор и оплата → /contract
- Реквизиты → /requisites
- Услуги → /services
```

## ⚠️ О TypeScript ошибках "Cannot find module"

Эти ошибки появляются потому что:

- Путь `@/*` в `tsconfig.json` указывает на `./src/*`
- Файлы находятся в `/Users/vladislav/testnext/src/`
- TypeScript сервер иногда не успевает индексировать новые файлы

**Решение:**

1. Перезапустите dev сервер: `Ctrl+C`, затем `npm run dev`
2. Или перезапустите TypeScript сервер в VS Code: `Cmd+Shift+P` → "TypeScript: Restart TS Server"

Код работает корректно! ✅

## 📝 Итого:

✅ Все красные ошибки TypeScript исправлены (добавлены типы)  
✅ Единая формулировка про оплату везде  
✅ Процесс работы обновлён (договор → оплата)  
✅ Footer использует данные из config  
✅ Логика одинаковая на `/contract` и `/services`

Всё готово! 🎉

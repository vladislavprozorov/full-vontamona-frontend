# 🎬 Hero Section - Техническая документация

## 📁 Структура файлов

```
public/
├── video/
│   ├── video-hero-trim.mp4    ← Основное видео
│   └── hero-poster.jpg        ← Превью (показывается до загрузки видео)
└── cruise-widget.html         ← HTML для виджета поиска

src/
├── components/
│   └── sections/
│       └── hero.tsx           ← Hero компонент с видео фоном
├── features/
│   └── pac-widget/
│       └── hero-widget.tsx    ← Виджет поиска для Hero
└── app/
    ├── page.tsx               ← Главная страница (Header variant="transparent")
    └── globals.css            ← Анимации fade-in
```

## 🎯 Основные фичи

### ✅ Что реализовано:

1. **Fullscreen видео фон**
   - Видео из `/video/video-hero-trim.mp4`
   - Poster image из `/video/hero-poster.jpg`
   - Автоплей, loop, без звука
   - Адаптивное масштабирование (object-cover)

2. **Форма поиска круизов** (вместо кнопок)
   - Интегрированный виджет cruise-widget.html
   - Полупрозрачный белый фон с backdrop-blur
   - Компактная высота (400px) для Hero
   - Автоматическая загрузка и индикация

3. **Оптимизация производительности**
   - `preload="metadata"` - загружаем только метаданные
   - `playsInline` - работает на iOS без fullscreen
   - Автопауза при неактивной вкладке (экономим ресурсы)

4. **Overlay градиент**
   - Затемнение `from-black/50 via-black/30 to-black/60`
   - Обеспечивает читаемость белого текста на любом фоне

5. **Анимации контента**
   - Fade-in для заголовка (0s)
   - Fade-in для формы поиска (0.3s)
   - Плавное появление снизу вверх

6. **Интерактивные элементы**
   - Форма поиска круизов (главный CTA)
   - Animated scroll indicator (стрелка вниз)
   - Скролл к результатам поиска

7. **Адаптивный дизайн**
   - `h-screen` - полная высота экрана
   - `min-h-150` (600px) - минимальная высота
   - Responsive текст: text-5xl → sm:text-6xl → md:text-7xl
   - Адаптивная форма поиска

## 🎨 Дизайн-система

### Цвета (из токенов):
```tsx
bg-brand-accent    // Оранжевый акцент для главной кнопки
text-white         // Белый текст на видео
border-white/30    // Полупрозрачные границы
bg-white/10        // Полупрозрачный фон с backdrop-blur
```

### Эффекты:
- `drop-shadow-2xl` - тень для текста
- `backdrop-blur-sm` - размытие фона кнопок
- `hover:scale-105` - увеличение при наведении
- `transition-all` - плавные переходы

## 🚀 Использование

### Базовое:
```tsx
import { Hero } from '@/components/sections/hero';

<Hero />
```

### С разными header вариантами:
```tsx
// Главная страница (transparent header)
<Header variant="transparent" />
<Hero />

// Внутренняя страница (solid header)
<Header variant="solid" />
<Content />
```

## ⚡ Оптимизация

### Текущий размер файлов:
- video-hero-trim.mp4: ~5-10 MB (зависит от длины)
- hero-poster.jpg: ~100-500 KB

### Рекомендации для production:

1. **Сжать видео** (см. VIDEO_OPTIMIZATION.md):
   ```bash
   ffmpeg -i video-hero-trim.mp4 -c:v libx264 -crf 23 -preset medium -an video-hero-optimized.mp4
   ```

2. **Создать WebM версию** (меньше на 30-50%):
   ```bash
   ffmpeg -i video-hero-trim.mp4 -c:v libvpx-vp9 -crf 35 -b:v 0 -an video-hero-trim.webm
   ```

3. **Добавить в код**:
   ```tsx
   <video ...>
     <source src="/video-hero-trim.webm" type="video/webm" />
     <source src="/video-hero-trim.mp4" type="video/mp4" />
   </video>
   ```

4. **Оптимизировать poster**:
   ```bash
   ffmpeg -i hero-poster.jpg -c:v libwebp -quality 80 hero-poster.webp
   ```

## 🎯 Performance метрики

### Целевые значения:
- **LCP** (Largest Contentful Paint): < 2.5s
- **FID** (First Input Delay): < 100ms
- **CLS** (Cumulative Layout Shift): < 0.1

### Оптимизации для LCP:
- ✅ Poster image загружается первым
- ✅ Video с `preload="metadata"`
- ✅ Критичный CSS inline
- ✅ Lazy loading для остального контента

## 🐛 Troubleshooting

### Видео не воспроизводится:

1. **Проверьте путь к файлу**:
   ```
   public/video-hero-trim.mp4 → доступен по /video-hero-trim.mp4
   ```

2. **Проверьте формат**:
   - MP4: H.264 кодек (поддерживается везде)
   - WebM: VP9 кодек (не работает в Safari < 14.1)

3. **iOS не запускает видео**:
   - Убедитесь что есть `playsInline`
   - Убедитесь что есть `muted`

4. **Видео тормозит**:
   - Уменьшите разрешение до 1080p или 720p
   - Увеличьте CRF (23 → 28) для меньшего размера
   - Удалите аудио дорожку (`-an`)

### Poster не показывается:
- Проверьте что `hero-poster.jpg` в `/public`
- Формат: JPEG или WebP
- Рекомендуемый размер: 1920x1080

## 🔮 Будущие улучшения

- [ ] Adaptive bitrate (разные версии для разных скоростей)
- [ ] Picture-in-Picture API
- [ ] Parallax эффект при скролле
- [ ] Замена видео на изображение для медленных соединений
- [ ] Добавить subtitles для accessibility
